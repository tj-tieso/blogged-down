<section class="d-flex flex-column px-3">
  <h2 class="pt-3">Create New Post</h2>

  <form class="new-post-form">
    <div>
      <label for="post-title">Title</label>
      <input type="text" id="post-title" name="post-title" />
    </div>
    <div>
      <label for="post-content">Content</label>
      <textarea type="text" id="post-content" name="post-content"></textarea>
    </div>
  </form>
    <button type="submit" class="btn btn-primary w-25 mx-3">Create</button>
</section>

{{#if posts.length}}
<section class="pt-3">
  <h2 class="text-dark px-3">Your Posts</h2>
  <ul class="post-list px-3" style="list-style-type:none">
    {{#each posts as |post|}}
    <li class="flex flex-column">
      {{> post-info post}}
      <a href="/dashboard/edit/{{post.id}}" class="edit-link btn btn-primary w-25 mx-3">Edit post</a>
    </li>
    {{/each}}
  </ul>
</section>
{{/if}}

<script src="/javascript/add-post.js"></script>